# 02 — Nuovo grafico "Spese mensili per Categoria" impilato al 100%

**What to build:** Nella pagina Dashboard, sezione "Uscite per Categoria", subito sotto il bar chart esistente, un nuovo grafico mostra come le Uscite si distribuiscono tra le Categorie mese per mese: una colonna verticale per mese, impilata per Categoria e normalizzata al 100% (l'altezza mostra sempre le proporzioni tra Categorie in quel mese, non l'importo assoluto speso). Usa lo stesso filtro di date indipendente già presente nella sezione (nessun filtro proprio), esclude sempre il mese solare in corso (dati parziali, coerente con `get_monthly_totals`), e mostra sempre tutte le Categorie note (nessun bucket "Altro").

**Blocked by:** 01

**Status:** done

- [x] Nuova funzione di servizio `get_monthly_category_breakdown(conn=None, date_from=None, date_to=None, today=None)`: aggrega le Uscite (esclusi Trasferimenti cross-bank ed Entrate) per (mese, Categoria) nel range di date indicato, esclude il mese solare corrente con lo stesso pattern già in uso in `get_monthly_totals` (parametro `today` iniettabile), restituisce una lista di dict `{"month": ..., "category": ..., "value": ...}` (una riga per ogni combinazione mese/Categoria con importo diverso da zero)
- [x] Test per `get_monthly_category_breakdown`: raggruppamento corretto per mese/Categoria, esclusione Entrate e Trasferimenti, esclusione del mese corrente (tramite `today` iniettato), range vuoto → lista vuota
- [x] Nuova funzione pura `domain.charts.monthly_category_breakdown_chart_data(breakdown: list[dict]) -> pd.DataFrame`: prende l'output di `get_monthly_category_breakdown`, restituisce un DataFrame in formato long con colonne Mese/Categoria/Totale (€)/Percentuale (la Percentuale è la quota della Categoria sul totale di quel singolo mese, non sul totale del periodo); con input vuoto restituisce un DataFrame vuoto
- [x] Test per `monthly_category_breakdown_chart_data`: su un breakdown noto con almeno 2 mesi e almeno 2 Categorie per mese, Totale (€) e Percentuale corretti per ciascuna riga (Percentuale calcolata sul totale del mese, non del periodo); input vuoto → DataFrame vuoto
- [x] Il grafico è reso con colonne verticali (una per mese, in ordine cronologico), impilate per Categoria con `stack` normalizzato al 100%
- [x] Ogni Categoria mantiene lo stesso colore già in uso nel bar chart sovrastante (stesso meccanismo a scale esplicito domain/range, per evitare il bug di scrambling colori già scoperto e risolto nel grafico esistente)
- [x] Il grafico mostra una legenda visibile con i colori delle Categorie (qui necessaria: l'asse mostra i mesi, non le Categorie)
- [x] Il tooltip on-hover su un segmento mostra Mese, Categoria, Totale (€) e Percentuale
- [x] Il grafico usa `category_date_from`/`category_date_to` già presenti nella sezione, nessun filtro data proprio
- [x] Quando `get_monthly_category_breakdown` non restituisce righe, viene mostrato lo stesso messaggio informativo già usato dal bar chart sovrastante ("Nessuna Uscita nel periodo selezionato."), invece del grafico
- [x] Verificato in browser: colonne mensili tutte della stessa altezza, segmenti colorati coerentemente col bar chart sovrastante, legenda leggibile, tooltip corretto su hover, mese corrente assente, filtro data della sezione invariato

## Comments

Implementato con TDD entrambi i seam puri indicati dalla spec: `get_monthly_category_breakdown` in `src/service.py` (riusa `_read_non_transfer_transactions` e il pattern `today`-iniettabile già in uso in `get_monthly_totals`/`get_monthly_cashflow_summary`), con 6 test in `tests/test_get_monthly_category_breakdown.py`; `monthly_category_breakdown_chart_data` in `src/domain/charts.py` (percentuale calcolata via `groupby("Mese").transform("sum")`, quindi sul totale del mese e non del periodo), con 2 nuovi test in `tests/test_charts.py`. La sotto-sezione "Spese mensili per Categoria" in `src/pages/dashboard.py` riusa lo stesso `category_color_scale` esplicito già costruito per il bar chart sovrastante e i filtri data `category_date_from`/`category_date_to` esistenti.

Code review a due assi: Spec pulita (nessuna lacuna, nessuno scope creep, nessuna implementazione errata). Standards ha segnalato 1 hard violation, risolta: la conversione data→ISO per `get_monthly_category_breakdown` duplicava letteralmente quella già scritta per `get_category_breakdown`, in contrasto con il precedente già presente nel file (`bar_date_from_iso`/`bar_date_to_iso`, calcolate una volta e riusate per due chiamate di servizio) — estratte `category_date_from_iso`/`category_date_to_iso`, riusate per entrambe le chiamate. Non applicati 2 judgement call minori: estrazione di un helper per il calcolo del mese corrente (`current_month = (today or pd.Timestamp.now()).strftime("%Y-%m")`, ora triplicato in `service.py`) — pattern preesistente riusato coerentemente con lo stile del file, refactoring delle due funzioni esistenti non pertinente allo scope di questa ticket; presunto accoppiamento fragile tra i due grafici tramite `category_color_scale` condiviso — in realtà sicuro, la variabile è costante (non dipende dai dati) ed è sempre definita nello stesso blocco `else` in cui viene usata, nessun rischio reale di `NameError`.

Verificato in browser (Streamlit dev server, dati reali già presenti nel DB) sia prima sia dopo il fix di code review: colonne mensili tutte alte 100%, colori dei segmenti coerenti col bar chart sovrastante (stesso `category_color_scale`), legenda "Categoria" leggibile a destra, tooltip on-hover mostra Mese/Categoria/Totale (€)/Percentuale (%) (confermato via screenshot, non solo via accessibility tree — quest'ultimo include anche i campi `x`/`y` grezzi non presenti nel tooltip visivo reale), mese corrente (2026-07, data odierna nel test 2026-07-19) correttamente assente, filtro data della sezione invariato.
