# Runtime + test dependencies for the expenses webapp and history migration.
# Pinned to the versions the project is developed against.

pandas==2.3.1
openpyxl==3.1.5        # Excel engine used by pandas.read_excel for the .xlsx exports
rapidfuzz==3.13.0      # fuzzy matching for expense categorization
streamlit==1.59.2      # webapp UI
altair==6.2.2          # explicit chart spec for the category bar chart (fixed category->color scale)
pytest==9.1.1          # test suite
